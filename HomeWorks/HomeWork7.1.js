/*function createSum() {
    let total = 0;
    return function (num) {
        total += num;
        return total;
    };
}

const sum = createSum();

console.log(sum(4));
console.log(sum(4));
console.log(sum(4));
console.log(sum(4));
*/

/*function createSum (initialValue) {
   let total = initialValue;
   return function (num) {
    total += num;
    return total;
   };
}

const sum = createSum (10);

console.log(sum(10));*/

/*function createSum () {
    let total = 0;
    let cals = 0;
    
    return function (num) {
     cals += 1;
     total += num;
     return {total , cals};
    };
 }
 
 const sum = createSum (10);
 
 console.log(sum(10));
 console.log(sum(20))
 */